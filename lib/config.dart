class AppConfig {
  // For Android Emulator: use 10.0.2.2 (special alias to host machine)
  // For iOS Simulator: use localhost
  // For Physical Device: use your machine's local IP (e.g., 192.168.1.15)
  static const String directusUrl = 'http://10.0.2.2:8061';
}